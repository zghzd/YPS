# 参数  
- `--workpath-<anypath>`指定一个目录作为工作目录，默认为./  
- `--mode-(selfcheck|file)`指定运行模式，默认为selfcheck  
- `--filetype-<filetype>`指定文件的类型的扩展名，默认为.  
- `--inputpath-<anypath>`指定一个目录作为输出目录，默认为./  
- `--pack-<packagename>`指定要加载的扩展包的名称，不同包用英文逗号`,`分割  
